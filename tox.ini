[tox]
envlist = py37

[testenv]
# install pytest in the virtualenv where commands will be executed
deps = -rdev_requirements.txt
commands =
    coverage run --source=cachet_url_monitor setup.py test
    coveralls
